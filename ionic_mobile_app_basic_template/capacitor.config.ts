import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_mobile_app_basic_template',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
